version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
permissions:
  - object: /
    pattern: "**"
    group: docker
hooks:
  ApplicationStart:
    - location: ./installApp.sh
      timeout: 300